import React, { Fragment } from 'react';

import AllListings from '../listings/AllListings';

const Listings = () => {
  return (
    <Fragment>
      <AllListings />
    </Fragment>
  );
};

export default Listings;
