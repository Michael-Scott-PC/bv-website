{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
        "database": "BriansDatabase",
        "host": "${process.env.DATABASE_HOST}",
        "srv": false,
        "port": 27017,
        "username": "${process.env.DATABASE_USERNAME}",
        "password": "${process.env.DATABASE_PASSWORD}"
      },
      "options": {
        "authenticationDatabase": "",
        "ssl": true
      }
    }
  }
}
