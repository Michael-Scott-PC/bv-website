{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
        "database": "${process.env.DATABASE_NAME}",
        "uri": "${process.env.DATABASE_URI}"
      },
      "options": {
        "ssl": true
      }
    }
  }
}
